spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/vehicleRentalSystem
    username: postgres
    password: argusadmin

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    time-zone: Asia/Kolkata

server:
  port: 8092

jwt:
  access-secret: "Java is a high-level, general-purpose, memory-safe, object-oriented programming language."
  refresh-secret: "Angular (also referred to as Angular 2+)[4] is a TypeScript-based free and open-source single-page web application framework."
  access-expiration-ms: 900000
  refresh-expiration-ms: 604800000

